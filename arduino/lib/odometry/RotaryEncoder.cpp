#include "RotaryEncoder.h"

RotaryEncoder::RotaryEncoder(uint8_t interrupt, float ratio) {
    // implement later
}

float RotaryEncoder::GetFrequency() const {

}

Encoder::Direction RotaryEncoder::GetDirection() const {
}
